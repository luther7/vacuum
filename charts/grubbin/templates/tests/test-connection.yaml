apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grubbin.fullname" . }}-test-connection"
  labels:
    {{- include "grubbin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grubbin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
